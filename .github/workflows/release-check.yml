name: release-check

on:
  workflow_dispatch:
  pull_request:
    paths:
      - CHANGELOG.md
      - docs/RELEASE_CHECKLIST.md

jobs:
  release-check:
    uses: Jonathhhan/ofxGgmlWorkflows/.github/workflows/release-check.yml@main
