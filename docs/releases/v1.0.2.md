# ofxGgmlLlama v1.0.2

## Compatibility

- openFrameworks: `of_v20260406_vs_64_release`
- minimum `ofxGgmlCore`: `1.0.1` / commit `2171e8b`
- `OFXGGML_LLAMA_VERSION_STRING`: `1.0.2`
- llama.cpp revision: `b9453` by default in `scripts/build-llama-server.*`

## Tested Runtime Backends

- CPU: build-script path covered by dry-run validation
- CUDA: rebuilt locally on Windows; `llama-cli` and `llama-server` report upstream commit `48b88c3`
- Vulkan: build-script path supported; local full inference smoke not part of this RC pass
- Metal: build-script path supported; local full inference smoke not part of this Windows RC pass
- OpenCL: build-script path supported; local full inference smoke not part of this RC pass

## Highlights

- Pinned the default llama.cpp runtime build revision to upstream release `b9453`.
- Rebuilt the local Windows CUDA runtime tools from release commit `48b88c3`.
- Preserved `-Revision <tag-or-commit>` override support for custom llama.cpp builds.

## Breaking Changes

- None.

## Migration Notes

- See `docs/MIGRATION.md` for Core/Llama split include changes.

## Validation

```text
scripts\validate-local.ps1
```

Result:

```text
ofxGgmlLlama local validation passed
```

Runtime version checks:

```text
llama-cli.exe --version: version: 1 (48b88c3)
llama-server.exe --version: version: 1 (48b88c3)
```

## Release Checklist

- `docs/RELEASE_CHECKLIST.md` completed
- no generated artifacts staged
- no models staged
- release tag created from this release-notes commit
